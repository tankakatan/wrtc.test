{
  "name": "chat-server",
  "version": "1.0.0",
  "description": "WS server for webrtc connection handshake",
  "main": "build/index.js",
  "scripts": {
    "preinstall": "cd ../shared && npm ci",
    "postinstall": "rm node_modules/shared && cp -Rf ../shared node_modules/shared && tsc",
    "start": "node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "takatan",
  "license": "ISC",
  "dependencies": {
    "shared": "file:../shared",
    "ws": "^7.4.0"
  },
  "devDependencies": {
    "@types/ws": "^7.4.0",
    "typescript": "^4.2.2"
  }
}
